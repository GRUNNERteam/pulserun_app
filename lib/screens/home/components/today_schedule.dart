import 'package:flutter/material.dart';

class TodayScheduleReminder extends StatelessWidget {
  const TodayScheduleReminder({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
