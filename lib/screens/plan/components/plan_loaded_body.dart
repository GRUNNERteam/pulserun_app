import 'package:flutter/material.dart';

class PlanLoadedBody extends StatelessWidget {
  const PlanLoadedBody({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Plan Loaded Body'),
    );
  }
}
